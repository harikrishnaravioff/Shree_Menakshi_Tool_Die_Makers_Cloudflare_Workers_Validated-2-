# Shree Menakshi Tool & Die Makers — Cloudflare Workers Static Site

Deploy command:

    npx wrangler deploy

Project layout:

- `wrangler.jsonc` — Workers Static Assets configuration
- `public/` — all website assets

The site is intentionally packaged without `.git`, `_headers`, build scripts, or nested site folders.
